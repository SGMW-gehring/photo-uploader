{
  "name": "photo-uploader-app",
  "version": "1.0.0",
  "description": "照片追溯上传 - 原生壳（Capacitor + ML Kit 扫码），加载 NAS Web 上传页",
  "private": true,
  "scripts": {
    "sync": "cap sync",
    "add:android": "cap add android",
    "add:ios": "cap add ios",
    "build:android": "cap sync android && cd android && ./gradlew assembleDebug",
    "build:ios": "cap sync ios && cd ios/App && pod install"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor-mlkit/barcode-scanning": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^5.4.0"
  }
}
